language: generic

os: linux
dist: focal
cache: packages
warnings_are_errors: false


before_install:
  - sudo apt-get install -y --no-install-recommends build-essential texlive-full

install: echo Installation step #do not remove this line!

script: make manual-git

after_success: tools/deploy.sh

notifications:
  email: false

